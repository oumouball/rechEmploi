{% extends 'base.html.twig' %}

{% block title %}Edit Cv{% endblock %}

{% block body %}
<div class="container col-md-5">
<div class="container mt-5">  
<div class="row">
   <div class="col-md-12 ">
     <div class="card">
             <div class="card-header bg-secondary">
             <h1> EDIT CV</h1>
            </div>
        </div>
	</div>
	</div>
	</div>

    {{ include('cv/_form.html.twig', {'button_label': 'Update'}) }}<br>
 
    <a class="btn btn-sm btn-secondary" href="{{ path('cv_index') }}">back to list</a> <br><br>
</th>
 <a class="btn btn-sm btn-danger"   {{  include('cv/_delete_form.html.twig') }} </a>
{% endblock %}
